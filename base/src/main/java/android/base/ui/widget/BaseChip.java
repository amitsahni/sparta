package android.base.ui.widget;

import android.content.Context;
import android.support.design.chip.Chip;
import android.util.AttributeSet;

public class BaseChip extends Chip {
    public BaseChip(Context context) {
        super(context);
    }

    public BaseChip(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public BaseChip(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
